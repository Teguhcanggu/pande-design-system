import * as React from 'react';

/** Small outlined pill for amenity keywords and filters. */
export interface TagProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}

export function Tag(props: TagProps): JSX.Element;
